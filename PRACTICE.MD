# הנחיות עבודה: דאשבורד קליטת עובדים

הקובץ הזה מסביר איך עובדים על הפרויקט: באיזה סדר, מי עורך איזה קובץ, ומה אסור להעלות לגיטהאב. כל מי שמצטרף לפרויקט, בן אדם או Claude, קורא אותו קודם.

## 1. על הפרויקט בקצרה

דאשבורד משאבי אנוש שמציג קליטת עובדים חדשים: מי נכנס, סטטוס משימות הקליטה, תקופת ניסיון וימי עבודה בחצי שנה הראשונה. הנתונים מגיעים מטבלת עובדים וקובץ נוכחות. כל הפרטים באפיון: [SPEC.MD](SPEC.MD).

## 2. סדר העבודה

תמיד באותו סדר. לא מדלגים על שלב.

1. **בקשה.** מנסחים מה רוצים: מה הדאשבורד צריך להראות, למי, ומאילו נתונים.
2. **שאלות מנחות.** לפני שבונים, Claude שואל שאלות הבהרה, ומשיבים עליהן. אם תשובה לא ברורה, מבקשים הסבר לפני שעונים.
3. **אפיון.** התשובות נכתבות ב־[SPEC.MD](SPEC.MD). מה שעוד לא הוחלט נכתב כשאלה פתוחה, עם ברירת מחדל.
4. **פיתוח.** בונים לפי האפיון בלבד. שינוי בדרישות מתחיל בעדכון האפיון, ורק אחר כך בקוד.
5. **בדיקה.** בודקים מול קריטריוני הקבלה בסעיף 12 באפיון, על נתוני דוגמה.
6. **פרסום.** רק אחרי שהבדיקה עברה ואחרי רשימת הבדיקה בסעיף 7 כאן.

## 3. איך מנסחים בקשה טובה

- **לכתוב מטרה, לא פתרון.** "אני צריכה לדעת מי לא השלים טופס 101" עובד טוב יותר מ"תעשי טבלה".
- **לציין קהל.** מי יפתח את הדאשבורד ובאיזו תדירות.
- **לצרף מבנה נתונים.** רשימת עמודות, ולא נתונים אמיתיים.
- **לבקש לשאול לפני שבונים,** כשיש החלטות פתוחות.
- **בקשה אחת בכל פעם.** קל יותר לבדוק ולתקן.
- **לתת תשובה ברורה.** "ברירת מחדל" מספיק כשהיא מתאימה. אם לא, לכתוב מה במקומה.

## 4. הקבצים ומי עורך מה

| קובץ | תפקיד | עורכים ידנית? |
|---|---|---|
| `SPEC.MD` | האפיון. מקור האמת לכל ההחלטות | **כן, זה הקובץ היחיד שעורכים** |
| `SPEC.docx` | גרסת Word של האפיון, להעברה לאחרים | לא. נוצר אוטומטית מ־`SPEC.MD` |
| `PRACTICE.MD` | הנחיות העבודה שלפניך | כן, כשמשתנה דרך העבודה |
| `dashboard/index.html` | הדאשבורד. גרסה שמתאימה לאפיון (נוכחות אמיתית, 6 חודשים, חריגות, סינון, ייצוא, עיצוב כחול וכתום). כרגע מציג נתוני דוגמה בלבד | כן, לפי האפיון |
| `dashboard/data/sample-*.csv` | קובצי נתוני דוגמה מומצאים שהדאשבורד טוען כברירת מחדל. אלה הקבצים היחידים מסוג CSV שמותר להעלות | נוצרים אוטומטית, לא עורכים ידנית |
| `tools/make-sample-data.ps1` | יוצר מחדש את קובצי הדוגמה כשהתאריכים בהם מתיישנים | רק אם צריך לשנות את הדוגמה |
| `tools/md2docx.ps1` | סקריפט שממיר את `SPEC.MD` ל־`SPEC.docx` | רק אם צריך לתקן את ההמרה |
| `.gitignore` | מונע העלאה של קבצי נתונים אמיתיים | רק בזהירות. ראו סעיף 5 |

**יצירת `SPEC.docx` מחדש** אחרי כל שינוי ב־`SPEC.MD` (ב־PowerShell, מתיקיית הפרויקט):

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools/md2docx.ps1 -In SPEC.MD -Out SPEC.docx
```

## 5. נתונים ופרטיות

בטבלאות של הפרויקט יש מידע רגיש על אנשים: שמות, תאריכי עזיבה, נוכחות ומספרי זהות. הכללים:

1. **נתונים אמיתיים לא נכנסים לפרויקט.** לא בקוד, לא באפיון, לא בדוגמאות, לא בהודעות קומיט.
2. **בדוגמאות ובבדיקות משתמשים בשמות ובמספרים מומצאים בלבד,** ורק בקבצים ששמם מתחיל ב־`sample-`.
3. **קבצי Excel ו־CSV לא עולים לגיט.** ה־`.gitignore` חוסם אותם, חוץ מקבצי `sample-`. אסור לעקוף אותו עם `git add -f`.
4. **מספר זהות לא מוצג ולא נשמר בדוגמאות.** אם צריך דוגמה, משתמשים בספרות מומצאות.
5. **הנתונים האמיתיים מועלים רק אחרי אישור בכתב** של ממונה הפרטיות או יועץ משפטי (שאלה 2 בסעיף 4 באפיון). כל עוד אין אישור, המאגר מכיל נתוני דוגמה בלבד.
6. **אם נתונים אמיתיים הועלו בטעות:** מפסיקים, לא מנסים למחוק בשקט, וקובעים בשיחה מה עושים. מחיקת קובץ לא מוחקת אותו מההיסטוריה של המאגר, ובמאגר ציבורי כל אחד יכול כבר לראות אותו.

## 6. עבודה עם גיט

- **מצב המאגר.** לפני כל קומיט: `git status`, ואחריו `git diff` על מה שעומד להישמר.
- **קומיט קטן וממוקד.** שינוי אחד בכל קומיט, ולא "הכול".
- **מוסיפים קבצים בשמם.** `git add SPEC.MD`, ולא `git add .`, כדי לא לצרף בטעות קובץ שלא התכוונו אליו.
- **הודעת קומיט:** משפט קצר ובלשון פעולה שאומר מה השתנה ולמה. למשל: "הוספת סעיף עיצוב גרפי לאפיון".
- **דוחפים רק אחרי בדיקה.** לפני `git push` מוודאים ש־`git status` נקי ושאין קבצים שלא אמורים לעלות.
- **בשום מקרה לא דוחפים את תיקיית AIDO.** יש בה קבצים אישיים. הפרויקט חי בתיקייה נפרדת, `hr-onboarding-dashboard`, עם מאגר משלו.

## 7. רשימת בדיקה לפני פרסום או דחיפה

- [ ] האפיון מעודכן, וכל שינוי בדרישות נכתב קודם ב־`SPEC.MD`.
- [ ] `SPEC.docx` נוצר מחדש מ־`SPEC.MD`.
- [ ] הדאשבורד נבדק מול קריטריוני הקבלה באפיון, על נתוני דוגמה.
- [ ] אין בשום קובץ שם, מספר זהות או נתון אמיתי של עובד.
- [ ] `git status` מציג רק את הקבצים שמתכוונים לשמור.
- [ ] כשהמאגר ציבורי: קראתי את הקבצים שעולים, כי כל אחד יראה אותם.

## 8. עיצוב

צבעים, גדלי אותיות ולחצנים מוגדרים בסעיף 10 באפיון: כחול וכתום על רקע ניטרלי, אותיות גדולות, לחצנים בולטים, ולא צבע בלבד להעברת מצב. לא ממציאים כללי עיצוב חדשים בקוד. משנים קודם את האפיון.
